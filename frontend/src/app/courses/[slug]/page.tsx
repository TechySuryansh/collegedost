import React from 'react';
import CourseDetailPage from './content';

export const metadata = {
    title: 'Course Details | CollegeDost',
    description: 'Detailed information about courses, career scope, top colleges, and entrance exams.',
};

const Page = () => {
    return <CourseDetailPage />;
};

export default Page;
